server {
  listen 80;

  server_name {{ site_name }};

  location / {
    return 301 https://$server_name$request_uri;
  }

  location /.well-known/ {
    root /opt/acme;
  }
}

server {
  listen 443 ssl;
}
